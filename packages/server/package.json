{
  "name": "server",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "node dist",
    "dev": "tsnd .",
    "build": "esbuild . --minify --bundle --outfile=dist/index.js --platform=node --format=cjs",
    "test": "echo \"Error: No test specified\" && exit 1"
  },
  "dependencies": {
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "esbuild": "^0.24.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
